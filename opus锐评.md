# Opus 锐评：Research Orchestra

> 独立审查视角，基于本地 `main`（提交 `56a84f3`）的只读核验。
> 与同目录 `gpt-sol锐评.md` 刻意采取不同切入点：不逐条复述工程缺陷，而是追问一个更上层的问题——**这个自称"自主科研"的系统，到底把可信度投在了哪里，又漏在了哪里。**
> 审查日期：2026-08-22。评分为主观判断，非测试输出。

---

## 总评：8.5 分的执行内核，装在一个 5 分的"自主性叙事"里

先给结论，再给证据。

Research Orchestra 是一个**工程质量明显高于平均水平的任务调度器**，但它给自己讲的故事是"自主科研编排"。这两件事之间存在一道裂缝：系统把最多的防护精力投在了"任务能不能被可靠地跑完"，而一个科研系统真正需要担保的是"跑出来的结论能不能被相信"。前者做得扎实，后者仍是软门禁。

这不是能力问题，是**优先级错配**。下面拆开讲。

| 维度 | 评分 | 一句话判断 |
|---|---:|---|
| 任务执行内核 | 8.5 | 状态机、重试、路径安全都是真代码，值得骄傲 |
| 结论可信度门禁 | 5.5 | 最该硬的地方（metrics schema）恰恰是软的 |
| 自主性（名副其实程度） | 4 | 自动化的是调度，不是科研判断 |
| 单一事实源 | 3.5 | 连锐评都会过期，说明没有权威真相 |
| 文档健康度 | 4.5 | 文档已从资产变成负债 |

---

## 一、被自动化的是"跑腿"，不是"科研"

系统名叫 orchestra，README 讲的是自主研究流水线。但把一次真实运行拆开看，人类需要亲手做的事包括：

- 写任务卡（executor、model、依赖、产物契约全是手填）；
- 锁定 Skill digest（`--lock-current`）；
- 执行部署（`deploy_broker.sh`）；
- 写验收报告（`reports/` 全靠人签字）；
- 维护 README / CLAUDE 里的测试数字和状态。

真正被机器接管的环节，是**任务的分派、重试、超时和归档**——也就是一个称职的 job runner 该做的事。而"这篇论文值不值得读""这个实验结论成不成立""排序对不对"这类**需要科研判断的决策**，要么交回给 LLM 现场发挥，要么交回给人。

夜间雷达的 `fetch → rank → render → notify` 拆分是这套系统里最接近"自主"的部分，方向也对（让 Python 做确定性排序、LLM 只做评分）。但它自动化的是"每天固定跑一遍并去重"，本质是**定时 + 幂等**，不是"系统自己判断哪篇重要"。评分标准仍是人写在 prompt 里的固定权重。

这不丢人——一个可靠的调度器已经很有价值。问题在于叙事：**当你把它叫做"自主科研"，用户会用"科研可信度"的标准来审判它，而它的防护体系并没有为这个标准而设计。**

---

## 二、防护重心和价值重心，装反了

这是我认为最尖锐的一点。

系统在"执行可信"上做得近乎偏执，全是实打实的代码：

- attempt 目录用 `mkdir(exist_ok=False)` 原子分配，防竞态；
- 产物路径拒绝绝对路径和 `..`，防逃逸；
- 超时用进程组树杀，防僵尸；
- 依赖缺失、成环、上游失败分别落 `invalid` / `blocked`，不永久卡队列。

这些都对，也都值钱。

但把镜头切到"科研可信"——一个科研系统的**核心资产**——防线却软了。实验数据摄入的 schema 校验长这样（`ingest_run.py`）：

```python
try:
    import jsonschema
except ImportError:
    jsonschema = None

if jsonschema is not None and SCHEMA_PATH.is_file():
    jsonschema.validate(data, load_schema())
```

`metrics.schema.json` 确实存在于仓库（我核验过，在 `academic-shared/research/`），但它的校验被**两道 optional 逻辑**架空：`jsonschema` 没装就跳过，schema 文件找不到也跳过。此时只剩四个顶层键、`exp_id` 正则和 status 枚举的浅检查。

于是形成一个荒诞的对比：

> 系统会**因为任务产物路径写了个 `..` 而 fail-closed**，
> 却可能**因为 `jsonschema` 没装而放行一份不合规的实验指标**。

前者是运维卫生，后者是科研命脉。防护强度却是反的。一个防止"文件放错地方"的系统，不应该比防止"数据不可信"的系统更严格。

**修法很短：schema 缺失、`jsonschema` 缺失，一律 fail-closed，并把 `metrics.schema.json` 纳入 Skill 的传递依赖 digest。** 这一改，系统的防护重心才和它的名字对齐。

---

## 三、连"锐评"都会过期，这本身就是最大的病

我在核验时发现一件很能说明问题的事。

同目录的 `gpt-sol锐评.md` 把"P0"级问题之一写成：

> `skills.json expected_digest=null → 真实 ingest 当前 HARD 阻断 / digest 未锁定

但我实际读到的 `config/skills.json` 里，两个 Skill 的 `expected_digest` **都已经是真实 64 位哈希，不再是 null**。也就是说，那份锐评在写下时或写下后不久就已经**部分失真**。

这不是要指摘 gpt-sol 那份——它在当时大概率是对的。真正值得警惕的是：**这个系统里，任何一份描述状态的文档，都会在下一次 commit 后悄悄过期，而没有任何机制会告诉你它过期了。**

README 说 116 个测试，CLAUDE 说 102 个，静态一数又是别的数字；digest 一会儿说 null 一会儿说锁定；同一份验收报告前半段和后半段结论打架。这些都不是笔误，是**同一个事实被手抄进了太多地方**的必然结果。

一个科研系统最该有的美德是"可复现的真相"。而现在它的真相散落在 README、CLAUDE、十几份 report、运行机状态和某人的记忆里，**没有一处是权威的**。文档越写越多，可判定性反而越来越低——文档从帮你理解系统，变成了另一个需要你去校对的状态数据库。

**我给的处方只有一句：测试数、commit SHA、Skill digest、部署版本、验收结论，全部由一条命令动态生成，Markdown 里只准放链接，不准放数字。** 锐评可以过期，但系统的"当前真相"不该靠人肉同步。

---

## 四、装饰性配置：看起来是方向盘，其实没接转向柱

`config/rules.yaml` 写得像模像样：

```yaml
default_executor: dsh
max_attempts: 2
degradation_order: [pi-dsh, win-dsh, direct]
```

我全仓搜了一遍，**除了 README 提到它，没有任何一行 Python 或 shell 读它**。真实行为是：executor 在任务卡里必填（没有"默认"这回事），`max_attempts` 来自 `broker/config.json`，降级顺序纯靠人手动操作。

这比"没有配置"更危险。因为运维者会打开 `rules.yaml`，改一行 `default_executor`，然后**以为系统行为变了**——实际上什么都没发生。`model-routing.json` 同理，没有 resolver 读它，任务卡里塞任意 `model` 字符串都能过。

配置文件的存在，是一种承诺："改我，行为会变。"当这个承诺是假的，它就成了一颗埋在运维路径上的哑雷。**要么接上转向柱，要么把方向盘拆了标成"仅供参考"。**

---

## 五、还没打扫干净的现场

一个小但很典型的证据。仓库里同时躺着两份同任务、同 attempt、结论相反的状态文件：

```
orchestra/results/T-20260819-e2e-dsh/attempt-2/state.json          → done
orchestra/results/results/T-20260819-e2e-dsh/attempt-2/state.json  → failed
```

第二份是历史上 `result: results/...` 路径双重拼接留下的疤。`.gitignore` 现在忽略了 `results/`，但**忽略规则管不了已经 tracked 的文件**，疤还在。人能凭上下文猜出哪份是真的，但任何自动聚合、审计或新接手的人，会得到一个**掷硬币级别的答案**。

科研系统里，"两份数据打架且都在仓库里"是比"没有数据"更坏的状态。**该做的是把冲突的历史 results 从版本控制里彻底清出去，验收证据改成只追加、带 hash 的独立 bundle。**

---

## 六、公允地说，它做对了什么

批评之后，必须把功劳给足，否则就是不诚实。

**Broker 内核是这套系统的灵魂，而且是活的。** `db.py` 的原子 claim、`dispatcher.py` 的依赖传播、`executor.py` 的超时与产物校验，不是架构图，是能处理异常路径的真实代码。很多 Agent 项目连"任务失败了该怎么办"都没想清楚，这里已经有 `blocked` / `invalid` 的明确终态。

**它对历史事故有真实的免疫记忆。** PID 复用、symlink 逃逸、attempt 竞态、Windows 进程树、通知模糊结果——每一个都对应一段"曾经被坑过"的代码，而不是"注意事项.md"。这说明系统真的在生产环境里跑过、疼过、改过。

**雷达的"松生成、严验证"方向完全正确。** 让 LLM 只做它擅长的判断，让 Python 兜住确定性和幂等，这是 LLM 编排该有的成熟姿态。

这些是硬功夫，别人抄不走。

---

## 七、如果只能改三件事

刻意和 gpt-sol 那份的建议错开，只挑我认为**最能修正"名实错配"**的三刀：

**第一刀，把防护重心搬回科研侧。** `metrics.schema.json` 校验改 fail-closed，纳入传递依赖 digest。让系统对"数据不可信"的警惕，至少不低于对"文件路径写错"的警惕。

**第二刀，立一个唯一事实源。** 一条 `orchestra_check.py`，动态吐出所有测试数、SHA、digest、部署版本、证据存在性。从此文档里不准手写状态数字。让"锐评会过期"这件事不再可能。

**第三刀，诚实地重命名叙事。** 要么把"自主科研"落到实处（让系统真的能自主判断论文价值、实验成败），要么坦然承认它现在是一个"很可靠的科研任务调度器"。名字配得上能力，用户才会用对的标准来信任它。

---

## 结语

Research Orchestra 的工程底子，配得上比现在更高的信任。但它当前把力气用在了"让火车准点"，而一个科研系统的乘客真正想知道的是"这趟车会不会把我拉到正确的目的地"。

下一步不该是加 Skill、加控制台、加报告。**该是把已经建好的可靠性，从执行层向上迁移到结论层——让这个系统不仅跑得稳，还担保得起它跑出来的每一个数字。** 做到这一步，它才配得上"科研"这两个字。
