# T-20260819-trt-dsh
executor: dsh
net: required
result: T-20260819-trt-dsh
timeout: 3600
---
你在执行科研对照实验 EXP-001 的 trt 臂（dsh 多步 agent 抽取）。Broker 已把你的进程工作目录设为输出目录（attempt-N，这就是你的沙箱 workspace——所有写入必须落在当前工作目录或其子目录，不要 cd 去别处写文件）。

素材与脚本（只读，绝对路径）：
- /mnt/broker/demo/material/extract_text.txt —— 待抽取文本（约150字虚构研究公告）
- /mnt/broker/demo/material/gold.json —— 金标准，格式 {"fields": {"<字段名>": "<标准值>"}}
- /mnt/broker/demo/score.py —— 共享评分脚本（只读执行，勿修改）

请按多步流程完成：
1. 读 extract_text.txt 与 gold.json，列出 gold 中的全部字段名
2. 逐步抽取：对每个字段，从文本中找到字面表述，逐字段核对（值必须与文本字面一致，strip 空白后精确比对）
3. 把抽取结果写成 result_trt.json 到当前工作目录，格式 {"fields": {"<字段>": "<值>"}}，键与 gold 完全一致
4. 运行评分：python3 /mnt/broker/demo/score.py --gold /mnt/broker/demo/material/gold.json --result result_trt.json --exp-id EXP-001 --out metrics.json
5. 报告 metrics.json 的绝对路径与 accuracy 值

注意：抽取由你（agent）自己完成，不要调用外部 API；metrics.json 由 score.py 产出（数值只来自 run artifact）。若某字段文本中确实找不到，写空字符串并说明。
